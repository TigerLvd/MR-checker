spring:
  profiles:
    active: test

gitlab:
  url: http://localhost:8089  # WireMock port for tests
  token: test-gitlab-token
  api-version: v4
  timeout: 5000

llm:
  url: http://localhost:8090  # WireMock port for LLM tests
  model: test-model
  timeout: 5000
  temperature: 0.0
  max-tokens: 1000

logging:
  level:
    com.mr.checker: DEBUG
